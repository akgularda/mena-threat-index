# MENA Threat Index — pipeline dependencies
# Lean by design: all statistics (AR(1), OLS, Newey-West, correlation, bootstrap)
# are implemented in numpy so we avoid statsmodels/pydantic, which keeps the
# pipeline fast and robust across Python versions in CI.
feedparser>=6.0.10
requests>=2.31.0
numpy>=1.26.0
pandas>=2.1.0
PyYAML>=6.0.1
